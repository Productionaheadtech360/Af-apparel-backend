{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE"
  },
  "deploy": {
    "startCommand": "sh -c \"echo 'Starting...' && alembic upgrade head && echo 'Migration OK' && uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000} --workers 1 --log-level debug\"",
    "restartPolicyType": "NEVER"
  }
}
